#include <string>
#include <vector>

using namespace std;

class Solution {
 public:
  int integerBreak(int n) {
    vector<int> maxN(n + 1);
    maxN[2] = 1;
    for (size_t i = 3; i < n; i++) {
      /* code */
    }
  }
};
